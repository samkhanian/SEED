# 🚀 تعليمات تشغيل سيستم الخبرة

## تم حل مشكلة بارگذاری پایگاه دانش! ✅

### ✨ ما تم إصلاحه:

1. **✅ تم إنشاء ملف `knowledge-data.js`**
   - يحتوي على جميع بيانات معرفة النظام مضمنة مباشرة
   - يتم تحميله تلقائياً قبل engine.js و app.js

2. **✅ تم تعديل `app.js`**
   - يدعم الآن تحميل البيانات من knowledge.json أو من البيانات المضمنة (fallback)
   - يعالج أخطاء CORS بشكل أفضل

3. **✅ تم تحديث `index.html`**
   - أضيفت علامة script لـ knowledge-data.js

---

## 🎯 كيفية التشغيل:

### الطريقة 1️⃣: فتح مباشر في المتصفح (الأسهل)
1. اذهب إلى مجلد `c:\Users\pc\SEED`
2. انقر بزر الماوس الأيمن على `index.html`
3. اختر **"فتح باستخدام"** → اختر متصفحك (Chrome, Firefox, Edge, etc.)
4. ✅ النظام سيعمل الآن!

---

### الطريقة 2️⃣: استخدام سرفر محلي (إذا أردت)

#### خيار A: باستخدام Python (إذا كان مثبتاً)
```powershell
cd c:\Users\pc\SEED
python -m http.server 8000
```

#### خيار B: باستخدام Node.js (إذا كان مثبتاً)
```powershell
cd c:\Users\pc\SEED
npx http-server
```

#### خيار C: باستخدام Powershell (مدمج في Windows)
```powershell
cd c:\Users\pc\SEED
powershell -Command {
    $listener = New-Object System.Net.HttpListener
    $listener.Prefixes.Add("http://localhost:8000/")
    $listener.Start()
    Write-Host "تم بدء السرفر على http://localhost:8000"
    while ($listener.IsListening) {
        $context = $listener.GetContext()
        # معالجة الطلب
    }
}
```

---

## 📊 الملفات الموجودة:

| الملف | الحجم | الغرض |
|------|-------|--------|
| `index.html` | 19 KB | صفحة الويب الرئيسية |
| `knowledge-data.js` | 21 KB | 🆕 بيانات المعرفة المضمنة |
| `knowledge.json` | 16 KB | بيانات المعرفة (النسخة الأصلية) |
| `engine.js` | 12 KB | محرك الاستدلال |
| `app.js` | 13 KB | منطق التطبيق |
| `style.css` | 15 KB | أنماط التصميم |
| `README.md` | توثيق شامل |
| `DESIGN.md` | مواصفات تقنية |

---

## ✅ اختبار السيستم:

1. افتح `index.html` في المتصفح
2. تأكد من ظهور النموذج بشكل صحيح
3. جرب إدخال البيانات:
   - بذر: **گندم رقم شیراز**
   - جوانه‌زنی: **92**
   - رطوبت: **12**
   - خلوص: **96**
   - منطقة: **فارس**
   - فصل: **بهار**
   - نوع خاک: **رسی**
4. انقر **"تحلیل و ارزیابی"**
5. يجب أن ترى النتائج مع الامتیازات والتوصیات ✅

---

## 🐛 استكشاف الأخطاء:

### إذا لم ير شيء:
1. افتح **وحدة التحكم** (F12) → **Console**
2. تحقق من الرسائل الخضراء والحمراء
3. تأكد من أن جميع الملفات في نفس المجلد

### إذا ظهرت رسالة خطأ:
```
❌ خطا در باركذاري پايگاه دانش!
```
- تأكد من أن `knowledge-data.js` موجود وليس فارغاً
- تحديث الصفحة (Ctrl+F5 أو Cmd+Shift+R)

### إذا لم يعمل الفتح المباشر:
- جرب الطريقة 2 (استخدام السرفر)

---

## 💡 معلومات مهمة:

- **النظام يعمل بدون اتصال بالإنترنت** ✅
- **جميع الحسابات تتم في المتصفح** ✅
- **بيانات المستخدم لا تُرسل إلى أي مكان** ✅
- **يعمل على جميع المتصفحات الحديثة** ✅

---

## 📞 عند حدوث مشاكل:

1. تحقق من أن جميع الملفات موجودة في `c:\Users\pc\SEED`
2. جرب تحديث الصفحة (Ctrl+F5)
3. جرب متصفح مختلف
4. احذف ذاكرة التخزين المؤقت (Cache)

---

**آخر تحديث:** 2024
**الحالة:** ✅ جاهز للاستخدام