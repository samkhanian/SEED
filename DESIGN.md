# توضیحات فنی سیستم خبره انتخاب بذر کاشت

## 📋 فهرست محتویات

1. [معماری کلی](#معماری-کلی)
2. [پایگاه دانش](#پایگاه-دانش)
3. [موتور استنتاج](#موتور-استنتاج)
4. [رابط کاربری](#رابط-کاربری)
5. [الگوریتم‌های استفاده‌شده](#الگوریتمهای-استفادهشده)
6. [جریان داده](#جریان-داده)
7. [توسعه‌های آتی](#توسعههای-آتی)

---

## معماری کلی

سیستم بر اساس معماری **سه‌لایه** طراحی شده است:

```
┌─────────────────────────────────────────┐
│   رابط کاربری (UI Layer)               │
│   HTML + CSS + JavaScript               │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│   موتور استنتاج (Engine Layer)         │
│   Inference Engine (JavaScript)         │
└──────────────────┬──────────────────────┘
                   │
┌──────────────────▼──────────────────────┐
│   پایگاه دانش (Knowledge Layer)         │
│   JSON Knowledge Base                   │
└─────────────────────────────────────────┘
```

### ویژگی‌های معماری:
- **Loose Coupling**: لایه‌های آزادانه از هم عمل می‌کنند
- **High Cohesion**: هر لایه مسئولیت مشخصی دارد
- **Scalability**: آسان‌تر افزودن قوانین یا بذرهای جدید
- **Maintainability**: تغییرات آسان در هر لایه

---

## پایگاه دانش

### ساختار فایل `knowledge.json`:

#### 1. **بخش بذرها (Seeds)**

```json
{
  "id": "wheat_shiraz",
  "name": "گندم رقم شیراز",
  "type": "غلات",
  "features": {
    "germinationRate": 92,      // درصد جوانه‌زنی
    "moisture": 12,              // درصد رطوبت
    "purity": 96,                // درصد خلوص
    "diseaseResistance": "زیاد", // مقاومت بیماری
    "color": "#D4A574"           // رنگ نمایشی
  },
  "suitableSeasons": ["بهار", "پاییز"],
  "suitableProvinces": ["فارس", "کرمان", "یزد"]
}
```

**معنی فیلدها:**
- `germinationRate`: درصد دانه‌هایی که قابل جوانه‌زنی هستند
- `moisture`: درصد آب موجود در بذر
- `purity`: درصد خلوص بذر (بدون ناخالصی)
- `diseaseResistance`: میزان مقاومت به بیماری‌های رایج

#### 2. **بخش مناطق جغرافیایی (Provinces)**

```json
{
  "id": "fars",
  "name": "فارس",
  "climate": "معتدل-خشک",
  "avgTemperature": 18,
  "avgRainfall": 450,
  "soilTypes": ["رسی", "لومی", "ماسه‌ای"],
  "coordinates": { "latitude": 29.6, "longitude": 52.5 }
}
```

**معنی فیلدها:**
- `climate`: نوع اقلیم منطقه
- `avgTemperature`: دمای میانگین سالانه (درجه سلسیوس)
- `avgRainfall`: میزان بارندگی سالانه (میلی‌متر)
- `soilTypes`: انواع خاک‌های موجود در منطقه

#### 3. **بخش قوانین (Rules)**

```json
{
  "id": "rule_001",
  "name": "قاعله انتخاب گندم در فصل بهار",
  "conditions": {
    "seedType": "گندم",
    "season": "بهار",
    "germinationMin": 85,
    "moistureMax": 14,
    "soilType": ["رسی", "لومی"],
    "avgTempRange": [15, 25]
  },
  "conclusion": "مناسب",
  "confidence": 95,
  "weight": 10,
  "recommendation": "بذر را فوری کاشت کنید"
}
```

**معنی فیلدها:**
- `conditions`: شرایطی که برای اعمال قاعله ضروری است
- `conclusion`: نتیجه اگر تمام شرایط برقرار باشند
- `confidence`: میزان اطمینان قاعله (0-100)
- `weight`: اهمیت نسبی قاعله در تصمیم‌گیری

#### 4. **بخش سطوح کیفی (Quality Levels)**

```json
{
  "germination": {
    "excellent": { "min": 90, "max": 100, "score": 100 },
    "good": { "min": 80, "max": 89, "score": 80 },
    "acceptable": { "min": 75, "max": 79, "score": 60 },
    "poor": { "min": 0, "max": 74, "score": 0 }
  }
}
```

این تعریف می‌کند که مقادیر مختلف چه امتیاز دریافت می‌کنند.

#### 5. **بخش وزن‌ها (Weights)**

```json
{
  "weights": {
    "germination": 0.35,          // 35% اهمیت
    "moisture": 0.25,             // 25% اهمیت
    "purity": 0.20,               // 20% اهمیت
    "diseaseResistance": 0.15,    // 15% اهمیت
    "environmentalFitness": 0.05  // 5% اهمیت
  }
}
```

---

## موتور استنتاج

### کلاس `InferenceEngine`

#### متدهای اصلی:

##### 1. `analyzeSeed(input)`
```javascript
const result = inferenceEngine.analyzeSeed({
  seedId: "wheat_shiraz",
  provinceId: "fars",
  germination: 92,
  moisture: 12,
  purity: 96,
  season: "بهار",
  soilType: "رسی",
  health: "سالم",
  diseaseResistance: "زیاد"
});
```

**بازگشت:**
```json
{
  "success": true,
  "seedName": "گندم رقم شیراز",
  "overallScore": 92,
  "verdict": "✅ بذر مناسب برای کاشت است",
  "details": {
    "germination": { "value": 92, "score": 100 },
    "moisture": { "value": 12, "score": 85 },
    "purity": { "value": 96, "score": 100 },
    "diseaseResistance": { "value": "زیاد", "score": 100 }
  },
  "recommendations": ["توصیه‌ها..."]
}
```

##### 2. `applyRules(input, seed, province)`
- قوانین مرتبط را جستجو می‌کند
- بررسی می‌کند که آیا شرایط قاعله برقرار است
- توصیه‌های مرتبط را جمع‌آوری می‌کند

##### 3. `calculateTotalScore(input, seed, province)`
**فرمول محاسبه امتیاز:**
```
Score = (Germination_Score × 0.35) +
        (Moisture_Score × 0.25) +
        (Purity_Score × 0.20) +
        (Disease_Score × 0.15) +
        (Environmental_Score × 0.05)
```

#### روند استنتاج:

```
شروع
  ↓
ورود داده‌های کاربر
  ↓
تصدیق صحت داده‌ها
  ↓
یافتن بذر و منطقه
  ↓
اعمال قوانین منطبق (Forward Chaining)
  ↓
محاسبه امتیازات بخشی
  ↓
تجمیع و محاسبه امتیاز نهایی
  ↓
تولید توصیه‌ها
  ↓
بازگشت نتیجه نهایی
  ↓
پایان
```

---

## رابط کاربری

### ساختار HTML

#### 1. Header (سرپوستیه)
- لوگو و عنوان سیستم
- اطلاعات درس و استاد

#### 2. Main Grid (محتوای اصلی)
##### الف. Form Section (سمت چپ)
- فرم ورودی با سه بخش:
  - انتخاب بذر
  - ویژگی‌های بذر (جوانه‌زنی، رطوبت، خلوص)
  - شرایط محیطی (منطقه، فصل، نوع خاک)
- دکمه‌های اعمال
- جعبه کمک‌آموزی

##### ب. Results Section (سمت راست)
- نمایش نتایج تحت سه حالت:
  - **Default State**: حالت پیش‌فرض
  - **Results State**: نمایش نتایج
  - **Error State**: نمایش خطا

#### 3. Footer (پاپوستیه)
- اطلاعات پروژه و تماس

### طراحی Responsive

```css
Desktop (>1024px):
┌─────────────────────────────────────────┐
│   Header                                │
├──────────────────┬──────────────────────┤
│   Form (50%)     │   Results (50%)      │
└──────────────────┴──────────────────────┘
│   Footer                                │
└─────────────────────────────────────────┘

Tablet (768px-1024px):
┌─────────────────────────────────────────┐
│   Header                                │
├─────────────────────────────────────────┤
│   Form (100%)                           │
├─────────────────────────────────────────┤
│   Results (100%)                        │
├─────────────────────────────────────────┤
│   Footer                                │
└─────────────────────────────────────────┘

Mobile (<768px):
┌─────────────────────────────────────────┐
│   Header (Compact)                      │
├─────────────────────────────────────────┤
│   Form (100%)                           │
├─────────────────────────────────────────┤
│   Results (100%)                        │
├─────────────────────────────────────────┤
│   Footer (Compact)                      │
└─────────────────────────────────────────┘
```

### رنگ‌بندی نتایج

| امتیاز | وضعیت | رنگ | Emoji |
|------|-------|-----|-------|
| ≥ 85 | مناسب | 🟢 سبز | ✅ |
| 70-84 | قابل‌قبول | 🟡 زرد | ⚠️ |
| < 70 | نامناسب | 🔴 قرمز | ❌ |

---

## الگوریتم‌های استفاده‌شده

### 1. Forward Chaining (استنتاج رو به جلو)

```javascript
FOR EACH rule IN rules:
  IF rule.conditions ARE MET WITH input AND seed AND province:
    ADD rule.conclusion TO results
    IF rule.recommendation EXISTS:
      ADD rule.recommendation TO recommendations
  END IF
END FOR
```

### 2. Weighted Scoring (امتیازدهی وزن‌دار)

```javascript
total_score = 0
FOR EACH factor IN factors:
  partial_score = getQualityScore(factor_value, levels)
  weighted_score = partial_score × factor_weight
  total_score += weighted_score
END FOR
final_score = MIN(MAX(total_score, 0), 100)
```

### 3. Pattern Matching (تطبیق الگو)

```javascript
FUNCTION ruleMatches(rule, input):
  FOR EACH condition IN rule.conditions:
    IF NOT matchCondition(condition, input):
      RETURN false
    END IF
  END FOR
  RETURN true
END FUNCTION
```

---

## جریان داده

### 1. بارگذاری اولیه

```
index.html لود شود
  ↓
app.js اجرا شود
  ↓
knowledge.json دانلود شود
  ↓
InferenceEngine ایجاد شود
  ↓
Selects پر شوند
  ↓
سیستم آماده کار
```

### 2. جریان تجزیه

```
کاربر فرم را پر کند
  ↓
دکمه "تحلیل" کلیک شود
  ↓
formData جمع‌آوری شود
  ↓
analyzeSeed() اجرا شود
  ↓
نتایج محاسبه شوند
  ↓
displayResults() اجرا شود
  ↓
نتایج نمایش داده شوند
```

---

## توسعه‌های آتی

### 1. بهبود موتور استنتاج
- اضافه کردن **Backward Chaining**
- پیاده‌سازی **Fuzzy Logic**
- اضافه کردن **Bayesian Networks**

### 2. یادگیری ماشین
- آموزش مدل‌های **Neural Networks**
- استفاده از **Decision Trees**
- بهبود پیش‌بینی با **Ensemble Methods**

### 3. بهبود رابط کاربری
- اضافه کردن **Dark Mode**
- پشتیبانی **چندزبانه** (انگلیسی، عربی و...)
- نمودارهای **تعاملی** (Interactive Charts)

### 4. ویژگی‌های جدید
- **پایگاه داده** برای ذخیره نتایج
- **API RESTful** برای استفاده دیگری
- **Notification System** برای هشدارها
- **Multi-language Support** برای بین‌الملل

### 5. بهتری‌های عملکردی
- **Caching** برای بهبود سرعت
- **Worker Threads** برای محاسبات سنگین
- **Progressive Web App (PWA)** برای استفاده آفلاین

---

## نتیجه‌گیری

این سیستم خبره:
- ✅ **معیاری**: بر اساس استانداردهای درس
- ✅ **قابل توسعه**: آسان‌تر اضافه کردن قوانین
- ✅ **کاربرپسند**: رابط ساده و شفاف
- ✅ **دقیق**: الگوریتم‌های درست‌الیاس
- ✅ **محدود**: شامل 20 قاعله و 11 نوع بذر

---

**نسخه**: 1.0  
**نویسنده**: پروژه درسی  
**تاریخ**: 1403  
**حوزه**: سیستم‌های خبره