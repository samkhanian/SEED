# ساختار پروژه بازسازی‌شده

## 📁 تغییرات اساسی

پروژه بر اساس معماری سیستم‌های خبره از نو طراحی شده است:

```
seed-expert-system/
│
├── 📄 README.md                 ← مستندات اصلی
├── 📄 STRUCTURE.md             ← این فایل
├── 📄 index.html               ← صفحه HTML اصلی
├── 📄 app.js                   ← برنامه اصلی و مدیریت رویدادها
├── 📄 style.css                ← استایل‌های تمام صفحات
│
├── 📂 data/                    ← پایگاه دانش (Knowledge Base)
│   ├── knowledge.json          ← دانش فکتی و قاعدی و متا
│   ├── seed-types.json         ← 20+ نوع بذر با ویژگی‌های دقیق
│   └── provinces.json          ← 8 استان ایران با داده‌های آب‌وهوایی
│
├── 📂 src/                     ← کد‌های هسته برنامه
│   ├── engine.js               ← موتور استنتاج (Inference Engine)
│   ├── ui.js                   ← مدیریت رابط کاربری (UI Manager)
│   └── utils.js                ← توابع کمکی و ابزارها
│
└── 📄 LICENSE                  ← مجوز
```

## 🧠 معماری سیستم

### 1. پایگاه دانش (Knowledge Base)
**فایل**: `data/knowledge.json`

شامل سه نوع دانش:

#### دانش فکتی (Factual Knowledge)
- اطلاعات ثابت درباره معیارهای ارزیابی
- مثال: "درصد جوانه‌زنی بالاتر از 85% نشانه بذر سالم است"
- موجود در بخش `factualKnowledge`

```json
{
  "id": "fact_001",
  "category": "معیار جوانه‌زنی",
  "content": "درصد جوانه‌زنی بالاتر از 85% نشانه بذر سالم و کاربردی است",
  "threshold": {"low": 75, "acceptable": 85, "excellent": 90},
  "weight": 10
}
```

#### دانش قاعدی (Rule-based Knowledge)
- قوانین IF-THEN برای تصمیم‌گیری
- مثال: "اگر جوانه‌زنی > 85 AND رطوبت < 14 THEN بذر مناسب است"
- موجود در بخش `ruleBasedKnowledge`

```json
{
  "id": "rule_001",
  "name": "قاعده انتخاب گندم در فصل بهار",
  "type": "selection",
  "priority": 1,
  "conditions": {
    "seedType": "گندم",
    "season": "بهار",
    "germinationMin": 85,
    "moistureMax": 14
  },
  "conclusion": "مناسب برای کاشت فوری",
  "confidence": 95,
  "weight": 10
}
```

#### دانش متا (Meta-knowledge)
- اطلاعات درباره اطمینان و اولویت قوانین
- مثال: "قانون 1 دارای اولویت بالاتر و اعتماد 95% است"
- موجود در بخش `metaKnowledge`

```json
{
  "priority": 1,
  "description": "قوانین بسیار مهم و ضروری",
  "rules": ["rule_001", "rule_003", ...],
  "weight": 1.0
}
```

### 2. موتور استنتاج (Inference Engine)
**فایل**: `src/engine.js`

#### روش استنتاج: Forward Chaining
شروع از داده‌های ورودی → بررسی قوانین → نتیجه‌گیری

#### عملکرد اصلی:

```javascript
// 1. استخراج فکت‌ها از ورودی کاربر
extractFacts(userInput)

// 2. محاسبه امتیاز بر اساس معیارهای مختلف
calculateScore(userInput)
  - scoreGermination()
  - scoreMoisture()
  - scorePurity()
  - scoreDiseaseResistance()
  - scoreTemperature()
  - scoreRainfall()

// 3. اعمال قوانین
inferenceForward(userInput)
  - بررسی شرایط هر قاعده
  - جمع‌آوری نتیجه‌ها
  - شناسایی هشدارها

// 4. تولید نتیجه نهایی
generateFinalConclusion(userInput)
  - تعیین وضعیت: "مناسب"، "قابل‌قبول"، "نامناسب"
  - تعیین رنگ
  - جمع‌آوری توصیه‌ها
```

### 3. رابط کاربری (User Interface)
**فایل**: `src/ui.js`

#### صفحات اصلی:

1. **صفحه خانه** (Home)
   - معرفی سیستم
   - ویژگی‌های کلیدی
   - آمار‌های پروژه

2. **صفحه تجزیه** (Analyzer)
   - فرم ورودی برای اطلاعات بذر
   - فرم ورودی برای شرایط محیط
   - نمایش نتایج

3. **صفحه اطلاعات** (Info)
   - توضیح معیارهای ارزیابی
   - جدول معیارهای استاندارد

4. **صفحه تاریخچه** (History)
   - تاریخچه تجزیه‌های قبلی
   - ذخیره در LocalStorage

## 📊 نوع‌های بذر

**فایل**: `data/seed-types.json`

موجود: **20+ نوع بذر**

ساختار هر بذر:
```json
{
  "id": "wheat_01",
  "name": "گندم رقم شیراز",
  "type": "غلات",
  "features": {
    "germinationRate": 92,
    "moisture": 12,
    "purity": 96,
    "diseaseResistance": "زیاد"
  },
  "requirements": {
    "temperatureRange": [15, 25],
    "rainfallMin": 300,
    "soilTypes": ["رسی", "لومی"]
  },
  "suitableSeasons": ["بهار", "پاییز"],
  "suitableProvinces": ["فارس", "کرمان"]
}
```

## 🌍 استان‌های ایران

**فایل**: `data/provinces.json`

موجود: **8 استان**

ساختار هر استان:
```json
{
  "id": "fars",
  "name": "فارس",
  "climate": "معتدل-خشک",
  "avgTemperature": 18,
  "avgRainfall": 450,
  "soilTypes": ["رسی", "لومی", "ماسه‌ای"],
  "factualData": {
    "averageHumidity": 35,
    "seasonalRainfall": { ... },
    "soilComposition": { ... },
    "dominantCrops": [...]
  }
}
```

## 📋 قوانین منطقی

**موجود: 15+ قاعده**

انواع قوانین:

### 1. قوانین انتخاب (Selection Rules)
مثال: "اگر شرایط خوب باشند → بذر مناسب است"

### 2. قوانین هشدار (Warning Rules)
مثال: "اگر رطوبت > 18% → نیاز به خشک‌سازی"

### 3. قوانین نگهداری (Maintenance Rules)
مثال: "اگر خلوص < 90% → نیاز به تمیزسازی"

## 🔄 جریان کار برنامه

```
کاربر وارد می‌شود
         ↓
فرم ورودی را پر می‌کند
         ↓
دکمه "تجزیه و تحلیل" را می‌زند
         ↓
app.js داده‌ها را جمع‌آوری می‌کند
         ↓
Utils اعتبار‌سنجی را انجام می‌دهد
         ↓
Engine استنتاج را اجرا می‌کند
         ↓
نمره محاسبه می‌شود
         ↓
قوانین بررسی می‌شوند (Forward Chaining)
         ↓
نتایج تولید می‌شوند
         ↓
UIManager نتایج را نمایش می‌دهد
         ↓
نتایج در LocalStorage ذخیره می‌شوند
```

## 🛠️ توابع کمکی (Utilities)

**فایل**: `src/utils.js`

- `loadKnowledgeBase()` - بارگذاری JSON
- `saveAnalysisResult()` - ذخیره نتایج
- `getAnalysisHistory()` - دریافت تاریخچه
- `exportToJSON()` - صادر کردن JSON
- `exportToCSV()` - صادر کردن CSV
- `validateInput()` - اعتبار‌سنجی ورودی
- `getImprovementSuggestions()` - پیشنهادات بهبود
- `getColorByScore()` - رنگ بر اساس نمره

## 📈 نمره‌دهی

نمره‌ها بر اساس وزن‌های زیر محاسبه می‌شوند:

| معیار | وزن |
|-------|-----|
| جوانه‌زنی | 10 |
| رطوبت | 10 |
| خلوص | 8 |
| مقاومت بیماری | 9 |
| دما | 8 |
| بارندگی | 7 |

نمره نهایی = (مجموع امتیازات وزنی) / (مجموع وزن‌ها)

## 💾 ذخیره‌سازی

- **LocalStorage**: ذخیره تاریخچه و نتایج
- **JSON Export**: دانلود نتایج
- **CSV Export**: جداول برای Excel

## 🎯 تغییرات اصلی از نسخه قبلی

1. **ساختار دایرکتوری منظم**: data/, src/ جداشده‌اند
2. **دانش فکتی و قاعدی جدا**: دستور‌العمل‌های واضح برای توسعه
3. **موتور استنتاج بهبود‌یافته**: Forward Chaining بهتر
4. **UI بهتر**: صفحات متعدد و ناوبری
5. **اعتبار‌سنجی ورودی**: بررسی دقیق
6. **توابع کمکی**: تابع‌های استفاده‌شده
7. **استایل واکنش‌پذیر**: برای موبایل

## 🚀 راه‌های توسعه

1. **اضافه کردن قوانین جدید**: معادل اضافه کردن به `knowledge.json`
2. **اضافه کردن بذرهای جدید**: معادل اضافه کردن به `seed-types.json`
3. **الگوریتم یادگیری**: می‌توان Machine Learning اضافه کرد
4. **پایگاه اطلاعات**: جایگزین JSON

---

**نسخه**: 2.0  
**تاریخ**: 1404/08/20  
**وضعیت**: بازسازی‌شده و بهتر شده